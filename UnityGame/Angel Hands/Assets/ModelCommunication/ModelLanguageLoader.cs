using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ModelLanguageLoader : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Load the selected_folders.txt file into a list in the game manager.
        //read json file.
        //get links to videos or files.
        //{word,category, link to video(starttime and endtime)}
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
